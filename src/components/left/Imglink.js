import React from 'react';
import '../../styles/Imglink.css'
// import './CounterControl.css';

const Imglink = (props) => (
        <img className= "gugl" src={props.im}/>
    );

export default Imglink;